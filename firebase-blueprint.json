{
  "entities": {
    "GateStatistic": {
      "title": "GateStatistic",
      "description": "Aggregated passenger statistics for a specific gate on a specific date.",
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "The date of the statistics in YYYY-MM-DD format."
        },
        "gateName": {
          "type": "string",
          "description": "The name of the gate (e.g., 'Gate 9')."
        },
        "totalPassengers": {
          "type": "number",
          "description": "Total number of passengers through this gate on this date."
        },
        "hourlyData": {
          "type": "object",
          "description": "Map of Hour Bucket (e.g., '12:00 - 13:00') to a map of Ticket Type to Count."
        },
        "laneData": {
          "type": "object",
          "description": "Map of Lane Name to Count and Duration."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last update."
        }
      },
      "required": ["date", "gateName", "totalPassengers", "hourlyData", "updatedAt"]
    },
    "DailySummary": {
      "title": "DailySummary",
      "description": "Aggregated daily summary for the entire facility.",
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "The date of the summary in YYYY-MM-DD format."
        },
        "totalPassengers": {
          "type": "number",
          "description": "Total number of passengers for the entire facility on this date."
        },
        "gateTotals": {
          "type": "object",
          "description": "Map of Gate Name to Total Count."
        },
        "hourlyTotals": {
          "type": "object",
          "description": "Map of Hour Bucket to Total Count (summed across all gates)."
        },
        "ticketTotals": {
          "type": "object",
          "description": "Map of Ticket Type to Total Count (summed across all gates)."
        },
        "gateHourlyTotals": {
          "type": "object",
          "description": "Map of Gate Name to its Hourly Breakdown (Gate -> Hour -> Total)."
        },
        "gateTicketTotals": {
          "type": "object",
          "description": "Map of Gate Name to its Ticket Breakdown (Gate -> Ticket -> Total)."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last update."
        }
      },
      "required": ["date", "totalPassengers", "gateTotals", "hourlyTotals", "ticketTotals", "updatedAt"]
    },
    "CableOperation": {
      "title": "CableOperation",
      "description": "Operation parameters for cable cars on a specific date.",
      "type": "object",
      "properties": {
        "date": {
          "type": "string",
          "description": "The date of operation in YYYY-MM-DD format."
        },
        "cableData": {
          "type": "object",
          "description": "Map of Cable ID to operation details (active, cabins, speed, hours)."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time",
          "description": "Timestamp of the last update."
        }
      },
      "required": ["date", "cableData", "updatedAt"]
    },
    "CableConfigs": {
      "title": "CableConfigs",
      "description": "Global configuration for all cable car lines.",
      "type": "object",
      "properties": {
        "configs": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": { "type": "string" },
              "name": { "type": "string" },
              "length": { "type": "number" },
              "maxCabins": { "type": "number" },
              "maxSpeed": { "type": "number" },
              "maxCapacity": { "type": "number" },
              "downtime": { "type": "number" },
              "enabled": { "type": "boolean" }
            }
          }
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        }
      },
      "required": ["configs", "updatedAt"]
    }
  },
  "firestore": {
    "gate_statistics/{statId}": {
      "schema": {
        "$ref": "#/entities/GateStatistic"
      },
      "description": "Stores daily aggregated passenger statistics per gate."
    },
    "daily_summaries/{date}": {
      "schema": {
        "$ref": "#/entities/DailySummary"
      },
      "description": "Stores daily aggregated summary for the entire facility."
    },
    "cable_operations/{date}": {
      "schema": {
        "$ref": "#/entities/CableOperation"
      },
      "description": "Stores daily operation parameters for cable cars."
    },
    "app_settings/cable_configs": {
      "schema": {
        "$ref": "#/entities/CableConfigs"
      },
      "description": "Stores the global configuration for all cable car lines."
    }
  }
}
